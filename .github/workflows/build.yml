name: Build

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v6
      - run: sudo xcode-select -s /Applications/Xcode_26.0.1.app/Contents/Developer
      - run: npm ci
      - name: Build app (unsigned)
        run: |
          set -o pipefail && xcodebuild -project alt-tab-macos.xcodeproj \
            -scheme Release \
            -configuration Release \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            -derivedDataPath DerivedData | scripts/xcbeautify
      - name: Ad-hoc codesign
        run: |
          codesign --force --deep --sign - DerivedData/Build/Products/Release/AltTab.app
          codesign --verify --verbose=2 DerivedData/Build/Products/Release/AltTab.app
      - name: Verify build
        run: file DerivedData/Build/Products/Release/AltTab.app/Contents/MacOS/AltTab
      - name: Package .app into zip
        run: ditto -c -k --keepParent DerivedData/Build/Products/Release/AltTab.app AltTab-unsigned.zip
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: AltTab-unsigned
          path: AltTab-unsigned.zip
